# Changelog

## [0.2.0] - 2026-08-01

### Added

- 持仓管理：CLI 子命令 `pos add/rm/list`；`pos add` 交互式多选监控列表股票并逐个录入股数与成本价（自动加入监控列表）。
- 持仓与收益显示：TUI 按 `t` 循环切换持仓、成本、持仓收益（率）、今日收益（率）列。
- 底部汇总：随 `t` 同步显示总市值/总成本、总收益（率）、今日收益（率）。
- 收益计算：持仓收益 = (现价 − 成本) × 股数；今日收益 = 涨跌额 × 股数。
- 收益列红盈绿亏独立着色。
- 全英文 UI：界面与 CLI 交互（含 `pos add` 提示）统一英文，强化伪装。

## [0.1.0] - 2026-07-30

### Added

- 初始版本发布。
- 终端实时行情展示（Rich 表格，红涨绿跌）。
- 自动刷新（交易时段 3 秒间隔，非交易时段停刷）。
- 老板模式：按 `b` 一键切换为 Docker build 伪日志，再按 `b` 切回。
- TUI 操作：`q` 退出，`r` 手动刷新。
- CLI 子命令：`add` / `rm` / `list` 管理监控列表。
- 配置持久化：`~/.astock.toml`。
- 离线状态检测与提示。
