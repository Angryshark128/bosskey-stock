# Changelog

## [0.4.0] - 2026-09-14

### Added

- **同名代码消歧**：所有要填代码的管理命令（`add` / `rm` / `group add` / `group add-codes` / `group rm-codes` / `pos rm`）会先探测该代码在沪/深/京三个交易所的行情 —— 只命中一个直接用；**命中多个（同名代码）不追问、不打断**，直接用段位规则那条，并把另一条候选连同它的强制前缀打成一行提示（`000001 = 平安银行（另有 上证指数 → sh:000001）`）；零命中提示「查无行情」而不再静默跳过；离线自动回退段位规则。（删除路径 `rm` / `group rm-codes` / `pos rm` 命中多条时仍列候选让用户选 —— 删除要明确。）
- **显式交易所前缀**：代码可写成 `sh:000001` / `sz:510300` / `bj:920002` 强制指定交易所（解决指数与股票同名，如 `sh:000001` 上证指数 vs `sz000001` 平安银行），只看该交易所、不再探测其它；与段位规则一致的前缀会被规范化，旧配置无需迁移。
- 监控列表 / 分组 / 持仓的键支持上述形式，`fetch()` 把响应回填为原存储键，行情、持仓与分组按同一键对齐；TUI 与 `list` 照原样显示键，两条同码可区分。
- **场外开放式基金支持**：用显式前缀 `fu:` 指定净值型开放式基金（支付宝 / 天天基金上按金额申购那种），如 `bosskey add fu:110022`（易方达消费行业股票）。走 Sina 场外接口（`fu:110022` → `fu_110022`，**下划线**连接），净值按 **4 位**小数显示。
- `fu:` 只能手写：`default_prefix()` 零改动，`fu` 永远不会被自动判定；裸代码探测仍是沪/深/京三个交易所（新增 `PROBE_EXCHANGES`），不会把「华夏成长混合A」这类场外基金混进 `add 000001` 的候选里。
- 显式 `fu:` 代码查不到净值时（代码不存在，或场外货币基金 —— 接口返回空串）给专门提示「查不到净值 —— 代码不存在，或为不支持的场外货币基金」，与普通「查无行情」区分。

### Changed

- 价格精度判定收敛到 `data.price_decimals()` 单一入口，行情层（涨跌额取整）与 TUI 渲染共用；原 `_is_etf` 更名为 `_is_fund` 并纳入深市 18 段，消除两处判定漂移。
- `price_decimals()` 新增 `fu:` → 4 位（场外基金净值本身 4 位小数）；股票 2 位、场内基金/债券 3 位不变。
- TUI 成本列精度改为可传参（`_fmt_price(v, dp=3)`，默认值不变），只有场外基金传 4 位（`_cost_decimals()`），其余品种显示零变化。
- 持仓份额支持小数：`pos add` 的份额提示由正整数改为正数（整数输入行为不变），TUI 与 `pos list` 里整数份额照旧（`100`）、小数份额显示 2 位去尾零（`352.4`）。场外按金额申购得到的就是小数份额（1000 元 ÷ 2.8377 ≈ 352.4 份）。
- `add` / `rm` 输出更明确：已在监控列表、不在监控列表、代码非法分别给提示；`rm` 未命中时不再打印「已移除」。
- i18n 取词函数的形参由 `key` 改为 `_key`，避免与文案占位符 `{key}` 冲突（`tr("...", key=...)` 会 `TypeError`）。
- **同名代码不再追问**：`add` / `group add` 命中多个时以前要按一次回车（列出候选让人选），现在直接落段位规则那条、零输入完成，另一条候选随提示给出（`_choose_quote` → `_pick_by_rule`）。实测会撞车的只有沪市 `000001`~`000999` 上证系列指数与深市同号段主板股票的交集，**共 204 个代码**，其余一律静默；这与基金无关，基金段位互斥、从不参与消歧。
- **帮助文本改为恒中文**：`--help` 不再走 i18n、也不随 `--lang` 切换。原先 `--lang zh add --help` 恒出英文 —— argparse 是在 `parse_args()` **内部**打印帮助，那一刻 `args.lang` 尚不存在，`_build_parser()` 只能拿到配置语言（为此加的 `_argv_lang()` 预扫补丁连同这个坑一起删除）。帮助是给人读的用法说明，不参与英文伪装，没必要让人先猜语言。argparse 自带的框架词一并中文化：`用法：` 前缀（覆写 `HelpFormatter.add_usage()`，argparse 里是硬编码）、`参数` / `选项` 两个小标题、`-h` 说明。
- `bosskey add --help` 补用法说明：解析规则、三种命中情形、`sh:` / `sz:` / `bj:` / `fu:` 前缀与四类示例。文案原在 i18n 表里，随「帮助中文化」移到 `__main__._HELP_ADD_EPILOG`。

### Fixed

- 深市场内基金小数位：180 段（REITs、封闭式基金，如 180101 蛇口产园、180301 盐港REIT）此前按 2 位小数显示，涨跌额被四舍五入截短（-0.012 显示为 -0.01），现按 3 位显示。沪市 508 段（如 508000 张江REIT）因属 5 段本就正确。
- 沪深 300（`sh000300`）等「段位规则落在无行情一侧」的代码此前既不显示也无提示，现由探测自动落到正确交易所或明确报「查无行情」。
- 场外基金净值/成本不再被 3 位精度截断（2.8377 曾显示为 2.838）；README 中 `110022` 一行「直接提示查无行情」的旧描述与实际不符，已按实测更正 —— 裸代码命中的是深市贴现国债 `sz110022`，会被加入并显示债券行情。


## [0.3.3] - 2026-09-07

### Changed

- **默认展示全部列**：启动即显示全部 14 列（mode 3，含持仓/成本、持仓盈亏（率）、今日盈亏），无需按键展开；`t` 键随之改为从全列逐层收起（原为从基础列逐层展开）。14 列全开建议较宽终端，`t` 可收起。

## [0.3.2] - 2026-08-19

### Changed

- **默认单色模式**：启动即单色（红涨绿跌着色关闭、全白显示），更贴合摸鱼伪装；`c` 键仍可会话内切换彩色/单色。
- 配置新增 `[display] colorize`：`false`（默认，单色）/ `true`（彩色）；旧配置无此键自动按单色处理。

## [0.3.1] - 2026-08-12

### Changed

- TUI 表格顶部新增黄色标题，始终显示当前视图（分组名或「全部」），`g` 切换后一眼可见所在分组；无分组配置时保持无标题。

## [0.3.0] - 2026-08-12

### Added

- 分组：监控列表支持分组管理，TUI 按 `g` 循环切换分组视图（组内独立排序、状态栏显示当前分组、底部收益汇总只统计当前分组）。
- CLI `group` 子命令：`group add/rm/rename/list/add-codes/rm-codes`；`add` 支持 `--group`（不存在自动建组）；`list` 支持 `--group` 过滤、`list -i --group` 组内交互排序。
- 分组名唯一性校验：重名/空名报错拒绝。

### Changed

- `rm` / `list -i` 删除代码时同步从所有分组移除，避免残留。
- 配置新增 `watchlist.groups`（旧配置自动兼容，视为全部未分组）。

## [0.2.5] - 2026-08-12

### Fixed

- 沪市新可转债代码段支持：713 开头（2024 后新上市可转债，如派克转债 713123）此前错误映射 `sz`，现正确映射 `sh` 前缀。注意：sina 行情源尚未收录部分新债，收录前该代码行情仍为空。

## [0.2.4] - 2026-08-12

### Added

- 债券支持：监控列表可直接添加债券/国债回购，行情解析与股票一致（沪市债券段 01/110/113/118/122/124/126/127/132/204 映射 `sh` 前缀，深市 10-13 段保持 `sz`）。

### Changed

- 债券/回购价格与涨跌额按 3 位小数取整，与价格列精度一致（如 952.725 不再显示为 952.72）。

## [0.2.3] - 2026-08-11

### Added

- 单色模式：TUI 内按 `c` 键切换红绿着色/全白显示，单色下收益列同步去色。
- 监控列表交互式管理：`bosskey list -i` 进入排序/删除界面（↑/↓ 或 k/j 移动，空格拿起/放下，d 标记删除，s 保存，q 取消）；`add`/`rm` 改为保留插入顺序。
- 北交所支持：4/8/92 开头代码映射 `bj` 交易所前缀。

### Changed

- ETF 涨跌额按 3 位小数取整，与价格列精度一致（如 -0.031 不再显示为 -0.030）。

## [0.2.2] - 2026-08-11

### Added

- ETF 支持：监控列表可直接添加 ETF，行情/持仓/收益计算与股票完全一致（深市 159 开头本就可加，本次修复沪市 5 开头如 510300/588000/518880 的交易所前缀映射）。
- ETF 价格 3 位小数显示：ETF/LOF（沪 5 开头、深 15/16 开头）净值精确到 3 位（4.728 不再四舍五入成 4.73），股票仍 2 位。

## [0.2.1] - 2026-08-06

### Added

- 中英界面切换：默认英文（维持伪装），TUI 内按 `l` 会话内切换，CLI 全局参数 `--lang {en,zh}` 单次覆盖，配置文件 `~/.bosskey.toml` `[display] lang` 设初始语言，`--list-langs` 列出支持语言。
- 快捷键提示：TUI 内按 `h` 开关底部快捷键提示行（默认隐藏，维持极简）。
- 文案集中到 `bosskey_stock/i18n.py`（key → (en, zh) 对照表），TUI（表头/状态栏/底部汇总/快捷键提示）与 CLI（add/rm/list/pos 输出、`pos add` 交互提示、`--help`）全部中英化。
- 老板模式日志主体保持英文 Docker 命令，仅底部 `Running time:` 本地化为 `运行时间:`。

### Changed

- 移除 mode 3 的 `TodayP/L%` 列（今日收益率与基础列 `Chg%` 数值相同，属冗余），模式 3 共 14 列，只保留 `TodayP/L` 今日收益额。

## [0.2.0] - 2026-08-01

### Added

- 持仓管理：CLI 子命令 `pos add/rm/list`；`pos add` 交互式多选监控列表股票并逐个录入股数与成本价（自动加入监控列表）。
- 持仓与收益显示：TUI 按 `t` 循环切换持仓、成本、持仓收益（率）、今日收益（率）列。
- 底部汇总：随 `t` 同步显示总市值/总成本、总收益（率）、今日收益（率）。
- 收益计算：持仓收益 = (现价 − 成本) × 股数；今日收益 = 涨跌额 × 股数。
- 收益列红盈绿亏独立着色。
- 全英文 UI：界面与 CLI 交互（含 `pos add` 提示）统一英文，强化伪装。

## [0.1.0] - 2026-07-30

### Added

- 初始版本发布。
- 终端实时行情展示（Rich 表格，红涨绿跌）。
- 自动刷新（交易时段 3 秒间隔，非交易时段停刷）。
- 老板模式：按 `b` 一键切换为 Docker build 伪日志，再按 `b` 切回。
- TUI 操作：`q` 退出，`r` 手动刷新。
- CLI 子命令：`add` / `rm` / `list` 管理监控列表。
- 配置持久化：`~/.astock.toml`。
- 离线状态检测与提示。
